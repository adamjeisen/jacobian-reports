**Success-criterion verdicts.** *Best run's leading Lyapunov exponent > 0*: **Pass** — the selected run (`n9kvy1ms`, LC=1e-5) gives λ₁ = +0.194 ± 0.047 from the 3 full-length test trajectories (and +0.377 from the 128 windowed estimate); every single run in the sweep also has λ_max > 0 (range +0.19 to +0.47). *Spectrum within ~30% of empirical*: **Fail**. Against the empirical estimate (λ ≈ [+0.272, −0.102, −13.84]), the best run matches λ₁ to ~29% and λ₃ to ~16%, but λ₂ = −0.852 is off by ~8× (wrong order of magnitude; the neutral flow direction is not recovered). *Sliced-Wasserstein-1 sanity*: **Unknown** — free-running SW₁ is not reported in `run_analytics.log` or `metrics.json` for this analysis. *val/trajectory_r2_score > 0.9*: **Pass** (R² = 0.9999 at best). *LC bounded and monotonically improving at low LC*: **Pass** — LC loss decreases strictly monotonically with LC weight (1.80 → 1.14 → 0.20 → 0.037 → 3.4e-3 → 6.2e-4 → 4.9e-5 → 9.5e-6 → 1.9e-6).

**Sweep landscape.** The sweep varies only `loop_closure_weight` (obs_noise_scale is fixed at 0, so there is no 2-D landscape to read). Best traj_val_loss forms a shallow U in log-LC with a minimum at LC = 1e-5 (5.6e-5) and a broad basin: LC ∈ {0, 1e-6, 1e-5, 1e-4} all sit within ~25% of the minimum (5.6e-5 – 6.9e-5), after which traj loss degrades ~2× by LC ≥ 1e-2. The Pareto frontier (6 runs) trades traj loss against LC loss smoothly; the pareto-knee is at LC = 1e-2, whereas traj-optimal is at LC = 1e-5.

**Lyapunov behaviour.** All 9 per-run spectra are qualitatively consistent: λ₁ > 0, λ₂ strongly negative (−0.71 to −1.64, never near zero), λ₃ between −12.5 and −18.1, and λ_sum uniformly negative (dissipative, no blow-up). Learned dynamics are therefore stable-yet-chaotic across the full LC sweep. No ground-truth Lyapunov reference was saved (`empirical_lyapunov_spectrum: null`, no `per_run_lyapunov_vs_true` figure), so only the sweep-internal comparison and the analytics-log empirical estimate are available.

**Caveats and hypothesis verdict.** All 9 runs completed cleanly; none were early-stopped, failed, or missing. The hypothesis that the sweet spot sits in LC ∈ [1e-3, 1e-1] is **not** supported — the traj-optimal LC sits three decades lower (1e-5), and the full-obs reference peak at 1e-1 did not transfer to the partial-obs regime. The broader hypothesis (LC-induced diffeomorphism → preserved Lyapunov spectrum) is **mixed/partially supported**: chaos is recovered and the contractive direction is in the right ballpark, but the neutral eigenvalue is missing. This is consistent with the stated open risk that z_dyn does not fully surface the history needed to reconstruct the full 3-D tangent geometry from x(t) alone, and suggests revisiting LPL / z_dyn-information pressure rather than further tuning LC.
