**Success criteria.** *Leading Lyapunov > 0 (chaos recovered)*: **Pass** — the best run (n9kvy1ms, LC=1e-5) gives λ₁ = +0.19 ± 0.05 on full-length test trajectories (+0.38 ± 0.46 on windowed), confidently positive. *Predicted spectrum within ~30% of empirical*: **Partial** — empirical is [+0.27, −0.10, −13.84], predicted [+0.19, −0.85, −16.10]; λ₁ is off by ~29% (borderline), λ₃ by ~16% (within), but λ₂ is off by roughly 8×, so the zero exponent of the flow direction is not recovered cleanly. *Free-running SW-1 low*: **Unknown** — the SW-1 / free-running-histogram computation bailed out with `NameError: name 'return_full_obs' is not defined` (see `metrics.json:analytics_error`), so this criterion is untested in this analysis. *val/trajectory_r2 > 0.9*: **Pass** — R² = 0.9999 at the best config, well above the threshold, and all nine runs exceed 0.9998. *Loop closure bounded and monotonically improving at low LC*: **Pass** — LC loss decreases monotonically with LC weight (1.80 → 1.14 → 0.20 → 0.037 → 0.0034 → 6.2e-4 → 4.9e-5 → 9.5e-6 → 1.9e-6) and stays finite everywhere.

**Landscape.** Because obs_noise_scale is held at 0, the (LC × noise) surface collapses to a 1-D LC sweep. Trajectory val loss is essentially flat and low across LC ∈ [0, 1e-4] (5.6e-5 to 6.9e-5), with a shallow minimum at LC=1e-5, then rises monotonically as LC grows (LC=10 → 1.26e-4, ~2.2× worst). The basin is broad on the low side and well-behaved; Pareto frontier (log 66) gives a full trade-off between traj loss and loop-closure tightness.

**Lyapunov across runs.** `fast_eigenvalue_fraction=0.0` for every run indicates no blow-up modes; learned dynamics are stable across the whole sweep. The per-run Lyapunov figure is available but `per_run_lyapunov` is empty in `metrics.json` and no empirical spectrum was attached (`true_lyapunov=null`, no per_run_lyapunov_vs_true plot), so cross-run spectrum tracking can only be eyeballed from the figure and not quantified here.

**Caveats.** All 9 runs finished cleanly, but five (LC ∈ {1e-5, 1e-3, 1e-2, 1, 10}) stopped exactly at the 200-epoch cap with `best_epoch` near the final epoch, suggesting loss was still trickling down at wall-clock stop — margins could shift with longer runs. The SW-1 failure is the main gap.

**Hypothesis verdict.** **Mixed.** Chaos is recovered and invertibility is enforced as expected, but the predicted optimum band (1e-3..1e-1) is wrong — the empirical sweet spot sits two to four orders of magnitude lower (LC=1e-5), and stronger LC actively hurts traj loss. The λ₂ mismatch is also consistent with the open "encoder dumps history into z_null" risk flagged in the hypothesis: z_dyn captures the unstable and fast-contracting directions but not the marginal flow direction cleanly.
