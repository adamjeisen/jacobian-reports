**Success criterion 1 — val/trajectory_r2_score >= 0.99: Fail.** The best R² across all 9 runs is 0.9889 (run `pip3w5po`, LC weight 1e-6), falling just short of the 0.99 threshold. The three lowest-LC-weight runs (0, 1e-6, 1e-5) all cluster tightly at R² ≈ 0.988–0.989, suggesting this is near the performance ceiling for the vanilla model at this noise level and epoch budget (8–12 epochs). **Success criterion 2 — val/trajectory_mase < 1.0: Pass.** The best MASE is 0.9038 (run `bi75wfwx`, LC weight 1e-5), comfortably below 1.0. Three runs achieve MASE < 1.0: LC weights 0 (0.928), 1e-6 (0.915), and 1e-5 (0.904), all within the low-LC regime. **Success criterion 3 — Cleaner loop-closure plateau: Unknown.** The analytics pipeline failed with a RuntimeError before producing figures, so no sweep-overview or LC-plateau plots are available for visual assessment. This criterion cannot be evaluated from the numeric summaries alone.

The LC weight landscape shows a clear monotonic pattern: trajectory quality degrades steadily as LC weight increases beyond 1e-5. The best trajectory losses live in the range LC ∈ {0, 1e-6, 1e-5} (traj loss ≈ 0.0097–0.0102), with performance already noticeably worse at 1e-4 (traj loss 0.0128, MASE 0.99) and deteriorating sharply at 1e-3 and above (MASE 1.33–2.82, R² dropping to 0.86 at LC=10). The basin of good performance is narrow — only the three smallest LC values produce MASE < 1.0 — and there is no evidence of an intermediate LC "sweet spot" that improves over pure trajectory fitting. Notably, the runs at LC ≥ 1e-2 also early-stopped at fewer epochs (5–8 at best-traj-loss epoch), indicating that high LC weight causes the trajectory loss to plateau or diverge early.

No Lyapunov spectra were computed (the `per_run_lyapunov` field is empty and no figures were generated), so we cannot assess whether the 30-step rollout better constrains subdominant Jacobian spectrum directions as hypothesized.

**Hypothesis verdict: Mixed / inconclusive.** The hypothesis that a 30-step horizon would better constrain Jacobians is not directly testable here because the analytics pipeline crashed before producing Lyapunov or Gramian diagnostics. On the trajectory-forecasting side, the model achieves strong MASE (0.90) and near-threshold R² (0.989), but falls marginally short of the 0.99 R² target. The analytics failure (wandb query returned no matching runs despite all 9 finishing cleanly) should be debugged and the analysis re-run to obtain the figures needed for a full evaluation — particularly the Lyapunov spectra that are central to the stated hypothesis.
